---
import Layout from '@layouts/Layout.astro';
import Card from '@components/Card.astro';
import data from '@data/RawMaterials.json';
---

<Layout title="Raw" slug="/raw">
	<main>
		{
			data.map((item) => (
				<div>
					{item.Name}
				</div>
			))
		}
	</main>
</Layout>
