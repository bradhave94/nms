---
import Layout from '@layouts/Layout.astro';
import Page from '@layouts/Page.astro';
import type { Item } from '@utils/lookup.js';
import { buildItemMeta } from '@utils/seo.js';
import data from '@datav2/Upgrades.json';

export async function getStaticPaths() {
	return data.map((upgrade) => ({
		params: { id: upgrade.Id.toString() },
		props: { item: upgrade },
	}));
}

const { item } = Astro.props;

const { title, description } = buildItemMeta(item as Item, 'Upgrade Module');
---

<Layout {title} {description} slug="upgrades">
	<Page item={item as Item} />
</Layout>
